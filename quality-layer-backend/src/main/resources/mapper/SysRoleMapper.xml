<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quality.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="com.quality.entity.SysRole">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role(name, code, description, status)
        VALUES(#{name}, #{code}, #{description}, #{status})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, name, code, description, status
        FROM sys_role
        WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT id, name, code, description, status
        FROM sys_role
        <where>
            <if test="name != null and name != ''">
                name LIKE CONCAT('%',#{name},'%')
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <update id="update">
        UPDATE sys_role
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_role WHERE id = #{id}
    </delete>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT id, name, code, description, status
        FROM sys_role
        WHERE code = #{code}
    </select>

    <select id="selectPermIdsByRoleId" resultType="java.lang.Long">
        SELECT permission_id FROM sys_role_permission WHERE role_id = #{roleId}
    </select>

    <select id="countUserByRoleCode" resultType="java.lang.Long">
        SELECT COUNT(1) FROM sys_user WHERE role = #{roleCode}
    </select>

</mapper>